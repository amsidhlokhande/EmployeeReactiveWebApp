# EmployeeReactiveWebApp

This example has Spring Web flux functional way of development of rest API's. 
It also includes Spring WebFlux Security implementation using JWT token 
It uses MongoDB database 
